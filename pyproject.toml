[project]
name = "retracesoftware_stream"
version = "0.1.12"
license = {text = "Apache-2.0"}
dependencies = []
#dependencies = ["retracesoftware_functional", "retracesoftware_utils"]

[build-system]
requires = ["meson-python>=0.15.0", "meson>=1.4", "ninja"]
build-backend = "mesonpy"